# Python dependencies for the reproducible pipeline (regression + figures).
# Pinned to the versions used by the authors (Python 3.12). The confidential
# traffic-processing notebooks are not shipped, so their cluster dependencies
# (pyspark, pydoop, mapply, geovoronoi) are intentionally omitted here.

# Core scientific stack
pandas==3.0.1
numpy==1.26.4
scipy==1.16.2
scikit-learn==1.7.2
statsmodels==0.14.2

# Plotting
matplotlib==3.10.6
seaborn==0.13.2
plotly==5.24.1

# Geospatial (commune maps; requires system GEOS / PROJ / GDAL)
geopandas==0.14.4
shapely==2.0.4
pyproj==3.6.1
mapclassify==2.6.1
folium==0.17.0

# I/O and utilities
openpyxl==3.1.5
xlrd==2.0.1
tqdm==4.67.1

# Notebook runtime
jupyter==1.1.1
ipykernel==6.30.1

# The Dirichlet regression stage (scripts/) runs in R, not Python:
#   R >= 4.0 with the DirichletReg package  ->  install.packages("DirichletReg")
